#' @param columns String. A vector of column names to keep. The rest of the
#'        columns are not read. Defaults to `NULL` and read all columns. If the
#'        user passes `merge_households = TRUE`, then the `columns` vector can
#'        also include the names of the households data set that should be kept.
