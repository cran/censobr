% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{delete_old_cache_dirs}
\alias{delete_old_cache_dirs}
\title{Delete files cached from previous data releases}
\usage{
delete_old_cache_dirs(verbose = TRUE)
}
\arguments{
\item{verbose}{A logical. Whether the function should print informative
messages. Defaults to \code{TRUE}.}
}
\value{
The paths deleted, invisibly.
}
\description{
The cache directory is versioned by data release
(\verb{\{cache_dir\}/data_release_\{tag\}}), so bumping the data release leaves the
files of the previous one behind. This deletes them.
}
\details{
Only directories named \verb{data_release_*} are considered, and the current
release is matched by equality rather than by pattern: the cache directory
can be any directory the user chose with \code{\link[=set_censobr_cache_dir]{set_censobr_cache_dir()}}, so
anything else living there is none of the package's business.

Microdata imported with \code{import_microdata22()} are never deleted.
They come from a zip file IBGE distributes under controlled access, so
censobr cannot download them again. A release directory that still holds
those files is kept.

Set \code{options(censobr.keep_old_cache = TRUE)} to disable this entirely, for
example to keep working with an older data release.
}
\keyword{internal}
